import React, {useState, useEffect} from 'react'
import '/Header.css'

const Hero = () => {
    const [data, setData] = useState(null);

useEffect(() => {
}, []);


  return (
    <div>Hero</div>
  );
  
  };
export default Hero